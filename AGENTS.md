# Expo HAS CHANGED

This project runs Expo SDK 54 (downgraded from the scaffold's SDK 57 to match
the team's installed Expo Go client). Read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code.
